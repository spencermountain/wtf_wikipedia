// const parsers = {
//   main: (section) => {
//
//   }
// };
//
const parseTemplates = function(obj) {
  // let names = obj.text.match(/^\{\{([^:|\n ]+)/g);
  // console.log(names);
  return obj;
};
module.exports = parseTemplates;
