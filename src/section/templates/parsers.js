const generic = function(wiki) {
  // templates.main = main[2].split('|');
  // wiki = wiki.replace(regs.main, '');
};

const parsers = {
  main: (tmpl) => {
  },
  wide_image: (tmpl) => {
  },
  tracklist: (tmpl) => {
  },
};

module.exports = parsers;
