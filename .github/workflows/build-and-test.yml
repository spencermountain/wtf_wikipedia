name: Build and test

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [24.x, 26.x]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm
      - name: Install, build, lint, and test
        run: |
          pnpm install --frozen-lockfile
          pnpm run build:all
          pnpm run lint
          pnpm run check
          pnpm run test:types
          pnpm test
          pnpm run testb
          pnpm run test:plugins
          pnpm run test:plugins:built
          pnpm run test:fetch
        env:
          CI: true
